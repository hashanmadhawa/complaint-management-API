{
  "name": "complaint-management",
  "private": true,
  "workspaces": ["backend", "complaint-frontend"],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run start --workspace backend\" \"npm run dev --workspace complaint-frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
